4 things left that move the needle on Mac. Everything else is tested or H100-only.

1. FIX XSA (failed from GQA shape bug, not because XSA doesnt work)
   Every legal PR above 1.10 uses XSA-all. The technique projects out the self-value
   component from attention output. Our attempt failed because V has shape [B,T,4,head_dim]
   (4 KV heads) but attention output is [B,T,8,head_dim] (8 heads). Need to broadcast V
   across GQA groups before the projection. Look at the competition train_gpt.py for
   reference implementation.

2. FIX FACTORIZED EMBED (failed from broken tied head, fix exists)
   8192x128 + 128x512 saves 2.3MB for extra layers. It broke because the output head
   assumes logits = x @ embed.T (single matrix). The fix is 2-matmul output:
   logits = x @ tok_emb_proj.weight.T @ tok_emb_small.weight.T
   Test at inner=128 first (safest), then inner=96.

3. IMPLEMENT EMA 0.997 (you said you did this in v2)
   Replace SWA with exponential moving average. After each step:
   ema = 0.997 * ema + 0.003 * weights
   Eval on ema weights. Competition standard, every top PR uses it.

4. STACK TABULATION HASH INTO ULTIMATE_V3
   b5 (tabulation hashing) gave -0.252 at 50 steps, our biggest win today.
   Its not in the ultimate_v2 stack yet. Build ultimate_v3 = ultimate_v2 + tabulation hash.
   Run 1000-step val to get new best.

After these 4, Mac testing is done. We move to H100.
